
from conductor.component.builder.serial import SerialBuilder
from conductor.component.builder.parallel import ParallelBuilder

builders = {
    "serial": SerialBuilder,
    "parallel": ParallelBuilder
}
